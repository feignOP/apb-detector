# APB Detector — Python dependencies
#
# OVITO must be installed via conda (not pip) to avoid Qt conflicts:
#   conda install -c conda-forge ovito
#
# All other packages are available via pip or conda:
#   pip install -r requirements.txt

numpy>=1.23
scipy>=1.9
matplotlib>=3.6
